# 毕设开发指南 — 完整版

> 基于 `feat/initial-logic-wip` 分支全部源码审查
> 审查日期：2026-03-21

---

## 项目核心价值定位

在开始修任何 Bug 之前，先明确你这个项目要在答辩时传递什么：

> **"用 AI 理解财经新闻的情感倾向，并可视化发现情感与股价之间的相关性。"**

这句话决定了所有开发优先级。你的核心亮点是：

1. **AI 情感分析能力** — SiliconFlow + Qwen2.5 对每条新闻实时分类，这是项目的"大脑"
2. **情感-股价相关性可视化** — OverlayChart 把两条线放在一起对比，这是项目独特的"洞察力"
3. **实时分析交互** — LiveAnalysis 让老师现场输入文本看到 AI 判断，这是答辩时最有冲击力的演示环节

所有修复和增强都围绕"让这三个亮点跑起来、看起来专业"展开。

---

## 第一部分：现有问题清单（按优先级排序）

### 🔴 P0 — 阻断级（不修就跑不起来）

| # | 问题 | 文件 | 现状 |
|---|------|------|------|
| B1 | `fetchCandles()` 用 Yahoo Finance，国内 403 | `backend/services/finnhubService.js:30` | StockChart + OverlayChart 永远空白 |
| B2 | `seed.js` 用 `db.run()`，better-sqlite3 无此方法 | `backend/scripts/seed.js:38,48,49,98,101,105` | CLI 种子脚本直接崩溃 |
| B3 | `App.jsx` API URL 写死绝对路径，绕过 Vite proxy | `frontend/src/App.jsx:8` | 开发时虽能跑（靠 CORS），部署时断开 |

### 🟡 P1 — 功能级（能跑但体验差或信息错误）

| # | 问题 | 文件 | 影响 |
|---|------|------|------|
| F1 | `CLAUDE.md` 6 处仍写 Groq API | `CLAUDE.md:5,7,25,55,91,107` | 老师翻代码发现文档和实际不一致 |
| F2 | `README.md` 3 处仍写 Groq API | `README.md:5,18,26` | 同上 |
| F3 | StockChart 只画 close 一条线 | `frontend/src/components/StockChart.jsx` | 图表单薄，不像专业金融仪表板 |
| F4 | OverlayChart 某条线为空时无提示 | `frontend/src/components/OverlayChart.jsx` | 用户不知道为什么只有一条线 |
| F5 | 首次打开全空，无引导 | `frontend/src/App.jsx` | 4 个灰色 "No data" 框，用户懵了 |
| F6 | `fetchAll` 用 Promise.all，一个失败全失败 | `frontend/src/App.jsx:35` | 股价 API 挂了连新闻数据也丢了 |

### 🔵 P2 — 优化级（时间充裕再做）

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| O1 | KeywordChart 只处理英文 | `KeywordChart.jsx:19` | `replace(/[^a-z\s]/g, '')` 会丢弃所有非英文字符 |
| O2 | `.env.example` 缺少 `ALPHA_VANTAGE_API_KEY` | `backend/.env.example` | 新人不知道还需要这个 key |
| O3 | news.js 用两次 SELECT 判断插入结果 | `backend/routes/news.js:57-59` | 可行但不够优雅 |

---

## 第二部分：数据源方案指导

### 当前问题

`fetchCandles()` 依赖 Yahoo Finance（`query1.finance.yahoo.com`），在中国大陆 IP 下返回 403。这是你所有图表空白的根本原因。

### 推荐方案：三级 Fallback

```
请求股价数据
    │
    ▼
┌─────────────────────────────┐
│  第 1 级：Alpha Vantage API  │  ← 需要免费 Key，JSON 格式，OHLCV 完整
│  25 次/天，国内基本可达       │
└──────────┬──────────────────┘
           │ 失败
           ▼
┌─────────────────────────────┐
│  第 2 级：Stooq CSV API     │  ← 无需 Key，CSV 格式，国内可访问
│  免费无限制                  │
└──────────┬──────────────────┘
           │ 失败
           ▼
┌─────────────────────────────┐
│  第 3 级：本地 Mock JSON     │  ← 内置 AAPL 30 天真实数据
│  答辩保险，网络全挂也不怕    │
└─────────────────────────────┘
```

### 各数据源详情

#### Alpha Vantage（推荐主源）

- **注册：** https://www.alphavantage.co/support/#api-key（免费，立即生效）
- **限制：** 25 次/天，5 次/分钟（毕设演示足够）
- **API 调用示例：**
  ```
  https://www.alphavantage.co/query?function=TIME_SERIES_DAILY&symbol=AAPL&outputsize=compact&apikey=YOUR_KEY
  ```
- **返回格式（JSON）：**
  ```json
  {
    "Time Series (Daily)": {
      "2026-03-20": {
        "1. open": "178.50",
        "2. high": "182.30",
        "3. low": "177.80",
        "4. close": "181.20",
        "5. volume": "52340000"
      }
    }
  }
  ```
- **注意：** 返回的 key 带数字前缀（`"1. open"`），解析时需要处理

#### Stooq CSV（备用源）

- **无需注册，直接调用**
- **API 调用示例：**
  ```
  https://stooq.com/q/d/l/?s=AAPL.US&d1=20260101&d2=20260320&i=d
  ```
- **返回格式（CSV）：**
  ```
  Date,Open,High,Low,Close,Volume
  2026-03-20,178.50,182.30,177.80,181.20,52340000
  ```
- **注意：** 美股 symbol 需要加 `.US` 后缀

#### 本地 Mock 数据（兜底）

- 在 `backend/data/` 下放一份 `mock-AAPL.json`
- 内容是提前保存的真实 AAPL 30 天历史数据
- 答辩当天断网也能正常演示

### fetchCandles 改写思路

```javascript
async function fetchCandles(symbol, from, to) {
  // 第 1 级：Alpha Vantage
  if (process.env.ALPHA_VANTAGE_API_KEY) {
    try {
      const data = await fetchFromAlphaVantage(symbol);
      if (data.length > 0) {
        console.log(`[Stock] Alpha Vantage: ${data.length} records`);
        return filterByDateRange(data, from, to);
      }
    } catch (e) {
      console.warn('[Stock] Alpha Vantage failed:', e.message);
    }
  }

  // 第 2 级：Stooq CSV
  try {
    const data = await fetchFromStooq(symbol, from, to);
    if (data.length > 0) {
      console.log(`[Stock] Stooq fallback: ${data.length} records`);
      return data;
    }
  } catch (e) {
    console.warn('[Stock] Stooq failed:', e.message);
  }

  // 第 3 级：本地 Mock
  console.warn('[Stock] All remote sources failed, loading mock data');
  return loadMockData(symbol, from, to);
}
```

### .env 更新

```env
FINNHUB_API_KEY=your_finnhub_key
SILICONFLOW_API_KEY=your_siliconflow_key
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key   # ← 新增
PORT=3000
```

---

## 第三部分：需要清理的过期信息

### CLAUDE.md（6 处必改）

| 行号 | 当前内容 | 改为 |
|------|----------|------|
| 5 | `performs sentiment analysis with **Groq API (LLaMA3)**` | `performs sentiment analysis with **SiliconFlow API (Qwen2.5-7B)**` |
| 7 | `Finnhub + Groq APIs` | `Finnhub + SiliconFlow + Alpha Vantage APIs` |
| 25 | `aiService.js # analyzeSentiment() via Groq` | `aiService.js # analyzeSentiment() via SiliconFlow` |
| 55 | `fill in FINNHUB_API_KEY and GROQ_API_KEY` | `fill in FINNHUB_API_KEY, SILICONFLOW_API_KEY, ALPHA_VANTAGE_API_KEY` |
| 91 | `Analyze arbitrary text via Groq` | `Analyze arbitrary text via SiliconFlow` |
| 107 | `GROQ_API_KEY=...` | `SILICONFLOW_API_KEY=...` 并新增 `ALPHA_VANTAGE_API_KEY=...` |

另外需要新增内容：
- Folder Structure 里补充 `backend/data/` 目录（Mock 数据）
- Folder Structure 里补充 `scripts/seed.js`
- Development Milestones 的 Status 从 "scaffold ready" 更新为实际进度
- Notes & Constraints 里加上 Alpha Vantage 25次/天限制说明
- Notes & Constraints 里把 "Groq free tier" 改为 "SiliconFlow free tier"

### README.md（3 处必改）

| 行号 | 当前内容 | 改为 |
|------|----------|------|
| 5 | `Groq (LLaMA3)` | `SiliconFlow (Qwen2.5)` |
| 18 | `GROQ_API_KEY` + "Groq console" | `SILICONFLOW_API_KEY` + SiliconFlow 地址 |
| 26 | `GROQ_API_KEY=your_key` | `SILICONFLOW_API_KEY=your_key` |

还需补充：
- `ALPHA_VANTAGE_API_KEY` 加入 API Keys 表格
- Features 表格里更新 Stock Price Chart 描述（三级 fallback）

### sprc.md

这个文件是旧的开发规划，方案已经被当前计划取代。建议：
- **不要删除**（保留开发记录对答辩有用，能展示迭代过程）
- 在文件顶部加一行：`> ⚠️ 此为早期规划草稿，已被最新开发方案取代`

---

## 第四部分：完整开发周期规划

### 阶段总览

```
Phase 1（半天）  修复阻断问题 → 项目能跑起来看到数据
Phase 2（1 天）  增强核心图表 → 答辩时视觉冲击力
Phase 3（半天）  UX 完善 → 空状态引导 + 布局升级
Phase 4（半天）  新增组件 → 锦上添花的答辩亮点
Phase 5（2 小时）文档清理 → 代码和文档一致性
```

---

### Phase 1：修复阻断问题（预计 3-4 小时）

> 目标：Fetch Data 后所有图表都能显示数据

#### 步骤 1.1：修复 fetchCandles — 三级 Fallback

**文件：** `backend/services/finnhubService.js`

**操作：**
- 删掉整个 Yahoo Finance 的 `fetchCandles` 函数
- 重写为三级 fallback：Alpha Vantage → Stooq CSV → 本地 Mock
- 新增三个内部函数：`fetchFromAlphaVantage()`、`fetchFromStooq()`、`loadMockData()`

**验证：** 启动后端，用 Postman 或浏览器访问：
```
GET http://localhost:3000/api/stocks/price?symbol=AAPL&from=2026-02-01&to=2026-03-20
```
应该返回 OHLC 数据数组，后端日志应显示用了哪个数据源。

#### 步骤 1.2：创建 Mock 数据文件

**操作：**
- 创建 `backend/data/mock-AAPL.json`
- 内容为 AAPL 过去 30 个交易日的真实 OHLC 数据（从 Alpha Vantage 手动获取一次存下来）
- 这确保答辩当天即使断网也能演示

#### 步骤 1.3：修复 seed.js

**文件：** `backend/scripts/seed.js`

**操作：** 全局替换 `db.run(` → `db.exec(`
- 第 38 行：`db.run('BEGIN')` → `db.exec('BEGIN')`
- 第 48 行：`db.run('COMMIT')` → `db.exec('COMMIT')`
- 第 49 行：`db.run('ROLLBACK')` → `db.exec('ROLLBACK')`
- 第 98 行：`db.run('BEGIN')` → `db.exec('BEGIN')`
- 第 101 行：`db.run('COMMIT')` → `db.exec('COMMIT')`
- 第 105 行：`db.run('ROLLBACK')` → `db.exec('ROLLBACK')`

**验证：** `node scripts/seed.js AAPL 2026-02-01 2026-03-20` 应该正常执行完毕。

#### 步骤 1.4：修复 App.jsx API URL

**文件：** `frontend/src/App.jsx`

**操作：** 第 8 行改为：
```javascript
const API = import.meta.env.VITE_API_URL || '';
```
空字符串让所有 fetch 走相对路径（如 `/api/news`），Vite proxy 正常接管。

**验证：** 浏览器 Network 面板里，API 请求应该走 `localhost:5173/api/...`（由 Vite proxy 转发），而不是直接请求 `localhost:3000`。

#### 步骤 1.5：更新 .env 和 .env.example

**操作：**
- `.env` 新增 `ALPHA_VANTAGE_API_KEY=你的key`
- `.env.example` 新增 `ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key`

**Phase 1 完成标志：** 启动前后端，点 Fetch Data → 4 个图表全部有数据。

---

### Phase 2：增强核心图表（预计 4-5 小时）

> 目标：图表从"能看"变为"专业"

#### 步骤 2.1：StockChart 增加 MA 均线 + High-Low Band

**文件：** `frontend/src/components/StockChart.jsx`

**增加内容：**

1. **MA5 / MA20 移动均线**（两条虚线）
   - 纯前端计算，遍历 close 数组，滑窗求平均
   - MA5 用浅色虚线，MA20 用深色虚线
   - 体现短期 vs 中期趋势

2. **High-Low Band**（波动范围半透明填充）
   - 用 Chart.js 的 `fill` 特性，在 high 和 low 之间填充半透明区域
   - 直观展示每日价格波动幅度

3. **改进 tooltip**
   - 显示 Open / High / Low / Close 四个价格
   - 显示 MA5 和 MA20 的值

**效果对比：**
```
之前：一条绿色 close 线 + 浅绿填充
之后：close 线 + MA5 虚线 + MA20 虚线 + high-low 半透明 band
```

#### 步骤 2.2：OverlayChart 增加部分数据提示 + 相关系数

**文件：** `frontend/src/components/OverlayChart.jsx`

**改动 1：** 区分三种状态
- 两条数据都有 → 正常显示
- 只有 sentiment → 显示提示："Stock data unavailable — showing sentiment only"
- 只有 price → 显示提示："No sentiment data — showing stock price only"

**改动 2：** 计算 Pearson 相关系数，显示在图表右上角

```javascript
// Pearson 相关系数（答辩亮点：量化情感与股价的关联程度）
function pearsonCorrelation(x, y) {
  const n = x.length;
  const sumX = x.reduce((a, b) => a + b, 0);
  const sumY = y.reduce((a, b) => a + b, 0);
  const sumXY = x.reduce((a, xi, i) => a + xi * y[i], 0);
  const sumX2 = x.reduce((a, xi) => a + xi * xi, 0);
  const sumY2 = y.reduce((a, yi) => a + yi * yi, 0);
  const num = n * sumXY - sumX * sumY;
  const den = Math.sqrt((n * sumX2 - sumX ** 2) * (n * sumY2 - sumY ** 2));
  return den === 0 ? 0 : num / den;
}
// 显示为：r = 0.73（强正相关）/ r = -0.45（中等负相关）
```

#### 步骤 2.3：改进 SentimentChart tooltip

**文件：** `frontend/src/components/SentimentChart.jsx`

**改动：** tooltip 里显示当天新闻数量 + 情绪分布（X 条正面 / Y 条负面 / Z 条中性）。
目前 tooltip 已经显示了 `count`，可以进一步从 newsData 中提取分布。

**Phase 2 完成标志：** StockChart 有 3 条线 + band，OverlayChart 有相关系数，视觉上像专业金融工具。

---

### Phase 3：UX 完善（预计 2-3 小时）

> 目标：消除"打开就是一堆空白"的困惑感 + 布局升级

#### 步骤 3.1：空状态引导

**文件：** `frontend/src/App.jsx`

**逻辑：** 当三个数据集全为空时，在图表区域上方显示引导横幅：

```jsx
{sentimentData.length === 0 && priceData.length === 0 && newsData.length === 0 && !loading && (
  <div className="empty-guide">
    <h2>👋 Welcome! Let's analyze some financial news.</h2>
    <p>Choose a stock symbol and date range above, then click
       the green "Fetch Data" button.</p>
    <button onClick={() => {
      setSymbol('AAPL');
      setFrom(daysAgo(30));
      setTo(todayStr());
      handleFetch();
    }}>
      Quick Start: AAPL, last 30 days
    </button>
  </div>
)}
```

#### 步骤 3.2：Summary Cards 组件

**新增文件：** `frontend/src/components/SummaryCards.jsx`

在图表上方显示 3 个数据概览卡片：

| 卡片 | 数据来源 | 显示格式 |
|------|----------|----------|
| 平均情绪得分 | sentimentData 均值 | `+0.42` 绿色 / `-0.31` 红色 |
| 分析新闻数 | newsData.length | `127 articles` |
| 趋势方向 | 近 7 天 vs 前 7 天均值 | `↑ Improving` / `↓ Declining` / `→ Stable` |

这些卡片让用户一进来就能"一秒读懂"当前状况，不用等看完所有图表。

#### 步骤 3.3：fetchAll 改用 Promise.allSettled

**文件：** `frontend/src/App.jsx`

```javascript
// 之前：一个失败全失败
const [sent, price, news] = await Promise.all([...]);

// 之后：各自独立，部分失败不影响其他
const [sentResult, priceResult, newsResult] = await Promise.allSettled([...]);
setSentimentData(sentResult.status === 'fulfilled' ? sentResult.value : []);
setPriceData(priceResult.status === 'fulfilled' ? priceResult.value : []);
setNewsData(newsResult.status === 'fulfilled' ? newsResult.value : []);
```

#### 步骤 3.4：布局从单列升级为 Grid

**文件：** `frontend/src/App.jsx`

当前的 `gridTemplateColumns: '1fr 1fr'` 已经是两列了，这其实还可以。
主要改进是**视觉层次**：

```
┌─ Summary Cards ───────────────────────┐  ← 首先看到数字
├─ Filters ─────────────────────────────┤  ← 然后操作
├─────────────────┬─────────────────────┤
│  SentimentChart  │  StockChart         │  ← 核心数据
├─────────────────┼─────────────────────┤
│  OverlayChart    │  KeywordChart       │  ← 深入分析
├─────────────────┴─────────────────────┤
│  News Table                            │  ← 原始数据
├───────────────────────────────────────┤
│  LiveAnalysis                          │  ← 交互区
└───────────────────────────────────────┘
```

建议调整顺序：把 OverlayChart（你的核心亮点）放在更突出的位置，或者让它在视觉上更大。

**Phase 3 完成标志：** 打开空白页面有引导，Summary Cards 立刻给出概览，布局有层次感。

---

### Phase 4：新增组件（预计 3-4 小时）

> 目标：锦上添花，增加答辩亮点

#### 步骤 4.1：SentimentDistribution 饼图

**新增后端路由：** `backend/routes/sentiment.js`

```javascript
// GET /api/sentiment/distribution?symbol=AAPL&from=...&to=...
router.get('/distribution', (req, res) => {
  const { symbol, from, to } = req.query;
  // 查询数据库，统计 positive/negative/neutral 各多少条
  // 返回 { positive: 42, negative: 28, neutral: 30 }
});
```

**新增前端组件：** `frontend/src/components/SentimentDistribution.jsx`

- Chart.js Doughnut Chart
- 三色：绿色(positive) / 红色(negative) / 灰色(neutral)
- 中心显示总数
- 这个图非常直观，老师一看就懂

#### 步骤 4.2：新闻列表增强（已有基础，小改）

当前 `App.jsx` 里已经有新闻表格了，可以小幅增强：
- 情绪标签加颜色背景（pill badge 样式）
- 表格加分页或"显示更多"按钮（新闻多时不会页面太长）
- 点击标题可以展开显示 AI 的分析理由（`reason` 字段）

**Phase 4 完成标志：** 仪表板有 6 个可视化组件 + 新闻列表 + 实时分析，信息密度达到答辩水准。

---

### Phase 5：文档清理（预计 1-2 小时）

> 目标：代码和文档完全一致

#### 步骤 5.1：更新 CLAUDE.md

按第三部分列出的 6 处逐一修改，另外更新：
- Folder Structure 补充新文件
- Milestones 状态更新
- Notes & Constraints 更新

#### 步骤 5.2：更新 README.md

按第三部分列出的 3 处修改，补充 Alpha Vantage key 说明。

#### 步骤 5.3：更新 .env.example

```env
FINNHUB_API_KEY=your_finnhub_key
SILICONFLOW_API_KEY=your_siliconflow_key
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key
PORT=3000
```

#### 步骤 5.4：sprc.md 标记为旧版

在文件顶部加：
```
> ⚠️ 此为早期规划草稿，最新方案见 project_status.md
```

#### 步骤 5.5：更新 project_status.md

反映当前实际进度和已修复的所有问题。

**Phase 5 完成标志：** 所有 `.md` 文件与实际代码一致，老师翻任何文档都不会看到 Groq。

---

## 第五部分：完整时间线

```
Day 1 上午 ─── Phase 1（修复阻断问题）
                ✓ fetchCandles 三级 fallback
                ✓ seed.js 修复 db.run
                ✓ App.jsx API URL 修复
                ✓ .env 更新
                【里程碑：所有图表能显示数据】

Day 1 下午 ─── Phase 2（图表增强）
                ✓ StockChart MA 均线 + High-Low Band
                ✓ OverlayChart 部分数据提示 + 相关系数
                【里程碑：图表看起来专业】

Day 2 上午 ─── Phase 3（UX 完善）
                ✓ 空状态引导横幅
                ✓ SummaryCards 组件
                ✓ Promise.allSettled
                ✓ 布局顺序调整
                【里程碑：用户体验流畅】

Day 2 下午 ─── Phase 4（新增组件）
                ✓ SentimentDistribution 饼图（后端 + 前端）
                ✓ 新闻列表小幅增强
                【里程碑：功能完整，可以准备答辩】

Day 3 上午 ─── Phase 5（文档清理）
                ✓ CLAUDE.md / README.md / .env.example 全部更新
                ✓ project_status.md 更新
                【里程碑：文档和代码一致】

Day 3 下午 ─── 最终验证 + 答辩准备
                ✓ 完整流程走一遍
                ✓ 断网测试 Mock 兜底
                ✓ 准备演示脚本
```

---



---

## 附录：改动文件清单

| 文件 | 改动类型 | Phase |
|------|----------|-------|
| `backend/services/finnhubService.js` | 重写 fetchCandles | Phase 1 |
| `backend/data/mock-AAPL.json` | **新增** | Phase 1 |
| `backend/scripts/seed.js` | 修复 db.run → db.exec | Phase 1 |
| `frontend/src/App.jsx` | API URL + 布局 + 空状态 + allSettled | Phase 1+3 |
| `backend/.env` | 新增 ALPHA_VANTAGE_API_KEY | Phase 1 |
| `backend/.env.example` | 新增 ALPHA_VANTAGE_API_KEY | Phase 1 |
| `frontend/src/components/StockChart.jsx` | MA 均线 + High-Low Band | Phase 2 |
| `frontend/src/components/OverlayChart.jsx` | 部分数据提示 + 相关系数 | Phase 2 |
| `frontend/src/components/SentimentChart.jsx` | tooltip 增强 | Phase 2 |
| `frontend/src/components/SummaryCards.jsx` | **新增** | Phase 3 |
| `frontend/src/components/SentimentDistribution.jsx` | **新增** | Phase 4 |
| `backend/routes/sentiment.js` | 新增 /distribution 接口 | Phase 4 |
| `CLAUDE.md` | 6 处 Groq → SiliconFlow | Phase 5 |
| `README.md` | 3 处 Groq → SiliconFlow | Phase 5 |
| `project_status.md` | 更新实际进度 | Phase 5 |
